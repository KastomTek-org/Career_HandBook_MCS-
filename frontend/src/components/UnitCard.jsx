import { Star } from 'lucide-react'
const colors = { 'Software Development':'blue', 'Business Computing':'green', 'Data and Information Systems':'purple', 'Professional Practice':'orange', 'Computing Foundations':'blue' }
export default function UnitCard({ unit, saved, onSave, onOpen }) { return <div className="card"><div className="row" style={{justifyContent:'space-between'}}><div><span className={`badge ${colors[unit.pathway]||'blue'}`}>{unit.pathway}</span><h3>{unit.code} — {unit.title}</h3><p className="muted">Year {unit.year} • Semester {unit.semester} • {unit.credits} credits</p></div><div className="row"><button className="btn secondary" onClick={onSave}><Star size={16} fill={saved?'currentColor':'none'}/> {saved?'Saved':'Save'}</button><button className="btn" onClick={onOpen}>Details</button></div></div></div> }
