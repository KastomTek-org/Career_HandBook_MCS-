export default function Curriculum({ controller }) {
  const { searchTerm, yearFilter, semesterFilter, bookmarks } = controller.state;
  return <main className="screen"><h2>Curriculum Structure</h2><div className="filters"><input placeholder="Search by code, title, or pathway" value={searchTerm} onChange={(e) => controller.setSearchTerm(e.target.value)} /><select value={yearFilter} onChange={(e) => controller.setYearFilter(e.target.value)}><option>All</option><option>1</option><option>2</option><option>3</option><option>4</option></select><select value={semesterFilter} onChange={(e) => controller.setSemesterFilter(e.target.value)}><option>All</option><option>1</option><option>2</option></select></div><div className="list">{controller.filteredUnits.map((unit) => <article className="card" key={unit.code}><small>Year {unit.year} • Semester {unit.semester} • {unit.credits} credits</small><h3>{unit.code} - {unit.title}</h3><p>{unit.pathway}</p><div className="actions"><button onClick={() => controller.toggleBookmark(unit.code)}>{bookmarks.includes(unit.code) ? "Saved" : "Bookmark"}</button><button onClick={() => controller.openUnit(unit)}>Details</button></div></article>)}</div></main>;
}
