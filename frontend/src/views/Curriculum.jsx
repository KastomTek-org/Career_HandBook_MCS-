import UnitCard from '../components/UnitCard'
export default function Curriculum({ controller }) { return <main className="container"><h2 className="section-title">Curriculum Structure</h2><div className="card"><div className="grid grid-3"><input className="input" placeholder="Search unit code, title, pathway" value={controller.query} onChange={e=>controller.setQuery(e.target.value)}/><select className="select" value={controller.year} onChange={e=>controller.setYear(e.target.value)}><option>All</option><option>1</option><option>2</option><option>3</option><option>4</option></select><select className="select" value={controller.semester} onChange={e=>controller.setSemester(e.target.value)}><option>All</option><option>1</option><option>2</option></select></div></div><div className="space"/><div className="grid">{controller.filteredUnits.map(unit=><UnitCard key={unit.id} unit={unit} saved={controller.bookmarks.includes(unit.id)} onSave={()=>controller.toggleBookmark(unit.id)} onOpen={()=>controller.openUnit(unit)}/>)}</div></main> }
