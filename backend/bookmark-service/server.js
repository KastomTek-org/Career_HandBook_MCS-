require('dotenv').config(); const express=require('express'); const cors=require('cors'); const routes=require('./routes/bookmarkRoutes'); const app=express(); app.use(cors()); app.use(express.json()); app.use('/api/bookmarks',routes); app.get('/health',(req,res)=>res.json({service:'bookmark',status:'ok'})); app.listen(process.env.PORT||5006,()=>console.log('Bookmark service running'));
