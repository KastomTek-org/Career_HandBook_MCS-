const fs=require('fs'); const path=require('path'); const file=path.join(__dirname,'../data/bookmarks.json'); const read=()=>JSON.parse(fs.readFileSync(file)); const write=d=>fs.writeFileSync(file,JSON.stringify(d,null,2)); exports.all=()=>read(); exports.add=unitId=>{const list=read(); if(!list.find(x=>x.unitId===unitId)) list.push({unitId}); write(list); return list;}; exports.remove=unitId=>{const list=read().filter(x=>x.unitId!==unitId); write(list); return list;};
