const fs=require('fs'); const path=require('path'); const file=path.join(__dirname,'../data/announcements.json'); const read=()=>JSON.parse(fs.readFileSync(file)); const write=d=>fs.writeFileSync(file,JSON.stringify(d,null,2)); exports.all=read; exports.create=data=>{const list=read(); const item={id:'a'+Date.now(), date:new Date().toISOString().slice(0,10), ...data}; list.unshift(item); write(list); return item;};
