require('dotenv').config(); const express=require('express'); const cors=require('cors'); const routes=require('./routes/handbookRoutes'); const app=express(); app.use(cors()); app.use(express.json()); app.use('/api/handbook',routes); app.get('/health',(req,res)=>res.json({service:'handbook',status:'ok'})); app.listen(process.env.PORT||5002,()=>console.log('Handbook service running'));
