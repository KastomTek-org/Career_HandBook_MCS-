require('dotenv').config(); const express=require('express'); const cors=require('cors'); const routes=require('./routes/careerRoutes'); const app=express(); app.use(cors()); app.use(express.json()); app.use('/api/careers',routes); app.get('/health',(req,res)=>res.json({service:'career',status:'ok'})); app.listen(process.env.PORT||5004,()=>console.log('Career service running'));
